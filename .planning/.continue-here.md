---
context: default
phase: null
task: 11
total_tasks: 16
status: in_progress
last_updated: 2026-07-04T15:30:21.902Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: `.env.local` gets destructively overwritten by `vercel env pull` / `vercel blob create-store` (it downloads and replaces the ENTIRE file with only what exists in Vercel's cloud env) — before running any `vercel` CLI command that touches env, back up `.env.local` first or be ready to reconstruct all local-only vars (auth, redis, resend, comment secrets) from scratch.
- [ ] CONSTRAINT: `GITHUB_REPO_BRANCH` must match an actual branch on the GitHub remote. The remote's only real branch is `codex/rutas-independientes` — there is no `main` or `master` pushed. Verify with `git ls-remote --heads origin` before assuming branch names.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting `.env.local.example` branch defaults | `.env.local.example` suggested `GITHUB_REPO_BRANCH=main` as a default; that branch doesn't exist in this repo's remote | blocking | Always run `git ls-remote --heads origin` to confirm real branch names before setting `GITHUB_REPO_BRANCH` |
| Running Vercel CLI commands that touch env without a snapshot | `vercel blob create-store` silently ran an implicit `env pull` and wiped 12 of 14 vars from `.env.local` | blocking | Read/copy `.env.local` content before any `vercel env` / `vercel blob` / `vercel link` command; restore immediately after if wiped |
| Pasting secrets in chat | User pasted GitHub OAuth Client Secret in plaintext twice | advisory | Secret is now compromised for production use; must rotate before deploying prod (see human_actions_pending) |

<current_state>
Panel admin (`/admin`) ya estaba construido en código (NextAuth v5 + Octokit) desde antes de esta sesión, pero le faltaban TODAS las credenciales de entorno. Esta sesión: (1) configuró GitHub OAuth completo para local, (2) depuró y arregló 2 bugs reportados por el usuario tras probar el panel: subida de imágenes rota, y guardado de posts en GitHub roto. Ambos bugs raíz resueltos. Falta que el usuario reinicie el server y confirme el último fix (branch) funciona end-to-end.
</current_state>

<completed_work>

Completed Tasks:
- Task 1-3: OAuth App de GitHub creada, vars en `.env.local`, login verificado funcionando en `/admin/login` — Done
- Task 4-7: Bug de upload de imagen — root cause: nunca existió un Vercel Blob store para el proyecto. Se linkeó el proyecto a Vercel (`apolo4/newsletter`), se creó el store `apolo-newsletter-images` (public), se linkeó, se recuperó `BLOB_READ_WRITE_TOKEN`. El comando de Vercel CLI sobreescribió `.env.local` por completo (efecto secundario no documentado de `vercel blob create-store`) — se reconstruyó el archivo completo a mano — Done
- Task 8: Ícono SVG de upload + texto guía "Click para subir una imagen (JPG, PNG, WEBP)" agregado en [PostForm.tsx](src/components/admin/PostForm.tsx:159) — Done
- Task 9-10: Bug de guardado en GitHub — root cause: `GITHUB_REPO_BRANCH=main` pero esa rama no existe en el remoto (verificado con `git ls-remote --heads origin`, único branch real: `codex/rutas-independientes`). Usuario eligió usar esa rama existente en vez de crear `main` nueva. Corregido en `.env.local` — Done
</completed_work>

<remaining_work>

- Task 11: Usuario reinicia dev server (Ctrl+C + `npm run dev`) para tomar `GITHUB_REPO_BRANCH` corregido — Not started
- Task 12: Reintentar guardar el post "PRueba" y confirmar que el commit llega a GitHub en la rama correcta — Not started
- Task 13: Aclarar si el comentario de prueba del usuario nunca confirmó el email (si no confirmó, no hay bug, es comportamiento esperado del flujo de confirmación) — Not started, pregunta pendiente sin responder
- Task 14: Producción — agregar las 8 env vars en Vercel dashboard (Settings → Environment Variables): `AUTH_GITHUB_ID`, `AUTH_GITHUB_SECRET`, `AUTH_SECRET`, `ADMIN_GITHUB_USERNAME`, `GITHUB_REPO_OWNER`, `GITHUB_REPO_NAME`, `GITHUB_REPO_BRANCH`, `BLOB_READ_WRITE_TOKEN` — Not started
- Task 15: Producción — crear una SEGUNDA GitHub OAuth App con callback del dominio real (GitHub OAuth Apps solo permiten 1 callback URL cada una) — Not started
- Task 16: **Seguridad** — rotar `AUTH_GITHUB_SECRET` actual (quedó expuesto en texto plano en el chat dos veces) antes de usarlo en producción — Not started
</remaining_work>

<decisions_made>

- Usar `codex/rutas-independientes` como `GITHUB_REPO_BRANCH` en vez de crear `main`, porque es la única rama real en el remoto y el usuario prefirió la opción rápida
- Vercel Blob store creado con `--access public` porque las imágenes de posts se muestran en `<img src>` público en el sitio
- Dos OAuth Apps de GitHub separadas (una para local, una para producción) porque GitHub no permite múltiples callback URLs en una sola app
</decisions_made>

<blockers>
- Verificación end-to-end de OAuth login y upload de imagen no se pudo automatizar por completo (el preview tool no puede completar un login real de GitHub) — el usuario probó manualmente y confirmó que ambos funcionan
</blockers>

## Required Reading (in order)
1. `.env.local` (no versionado) — contiene todas las credenciales actuales; branch acaba de corregirse a `codex/rutas-independientes`
2. [src/lib/auth.ts](src/lib/auth.ts) — config de NextAuth, scope `read:user repo`, gate por `ADMIN_GITHUB_USERNAME`
3. [src/lib/github.ts](src/lib/github.ts) — `commitPost`/`deletePost`, usan el `accessToken` de sesión + `GITHUB_REPO_OWNER/NAME/BRANCH`
4. [src/app/api/admin/posts/route.ts](src/app/api/admin/posts/route.ts) — swallows el error real de GitHub en un mensaje genérico "No se pudo guardar en GitHub"; si vuelve a fallar, revisar logs de servidor (`console.error('commitPost failed:', err)`), no solo la UI

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: Asumir que `GITHUB_REPO_BRANCH=main` es correcto porque es el default convencional → siempre verificar con `git ls-remote --heads origin` contra el remoto real antes de fijar env vars de branch
- [ANTI-PATTERN]: Correr comandos `vercel env pull` / `vercel blob create-store` / `vercel link` sin respaldar `.env.local` antes → estos comandos pueden sobreescribir el archivo completo con solo lo que existe en el entorno cloud de Vercel

## Infrastructure State
- Dev server: normalmente corrido por el usuario en su propia terminal en `localhost:3000` (no el preview-managed server) — falta reiniciarlo para tomar el branch corregido
- Vercel: proyecto `apolo4/newsletter` linkeado localmente (`.vercel/project.json` existe); Blob store `apolo-newsletter-images` (`store_OjpCKKKwrMIQkXd4`) creado y conectado, access public
- GitHub: OAuth App de desarrollo creada (Client ID `Ov23liRTfyYWKG2PFIrZ`), callback `http://localhost:3000/api/auth/callback/github`
- `.env.local`: contiene 14 vars — Redis/Upstash, Resend, ADMIN_TOKEN, COMMENT_SESSION_SECRET, AUTH_GITHUB_ID/SECRET, AUTH_SECRET, ADMIN_GITHUB_USERNAME, GITHUB_REPO_OWNER/NAME/BRANCH (corregido a `codex/rutas-independientes`), BLOB_READ_WRITE_TOKEN, VERCEL_OIDC_TOKEN

<context>
El panel admin ya estaba completamente codeado antes de esta sesión (commits previos: `feat: add admin pages for posts CRUD and comment moderation`, `feat: add shared PostForm component`, `feat: add image upload route via Vercel Blob`). Esta sesión fue pura configuración de infraestructura + debugging sistemático, sin tocar arquitectura. El enfoque fue: (1) nunca adivinar, siempre verificar contra el remoto real (branches, tokens, stores) antes de aplicar fixes, (2) usar la skill de systematic-debugging para encontrar root cause antes de proponer arreglos — ambos bugs se resolvieron con un solo fix cada uno, sin parches sintomáticos.
</context>

<next_action>
Start with: pedirle al usuario que reinicie `npm run dev`, vuelva a loguear en `/admin/login`, reintente crear el post "PRueba" y confirme si el commit llegó a GitHub en la rama `codex/rutas-independientes`. Si falla de nuevo, revisar el log de servidor real del usuario (no el preview tool) para ver el error exacto de Octokit.
</next_action>
