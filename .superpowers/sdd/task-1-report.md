# Task 1 Report

## Resumen
- Extendí `src/lib/categories.ts` con `RouteLayout`, `RouteExperience`, `CategoryMeta.routeExperience` y `getRouteHref(category)`.
- Añadí `routeExperience` a las siete categorías existentes con los valores pedidos por el brief.
- No agregué dependencias ni toqué otros archivos de la app.

## Verificación
- `npm run lint` completado.
- Resultado: 0 errores; quedaron 3 advertencias preexistentes de `@next/next/no-img-element` en otros archivos fuera de este task.

## Notas
- El archivo de categorías ya contenía texto con codificación heredada; mantuve ese patrón para no introducir cambios ajenos al task.
